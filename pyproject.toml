[project]
name = "powetsy"
version = "0.1.0"
description = "POWEtsy — single-purpose physical AI agents with personality"
requires-python = ">=3.11"
dependencies = ["requests>=2.31"]

[project.optional-dependencies]
dev = ["pytest>=7.0", "ruff>=0.1"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
